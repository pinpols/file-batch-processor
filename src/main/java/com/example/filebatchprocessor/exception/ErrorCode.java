package com.example.filebatchprocessor.exception;

public enum ErrorCode {
    INVALID_ARGUMENT,
    NOT_FOUND,
    VALIDATION_FAILED,
    IO_ERROR,
    NETWORK_ERROR,
    DB_ERROR,
    INTERNAL_ERROR
}
